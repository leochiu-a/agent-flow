name: jira-mcp
workflow:
  - name: list jira projects
    agent: claude
    prompt: >-
      Use the Jira MCP tools to list projects. Return total count and the first
      50 projects.
    skip_permission: true
claude_session: shared
